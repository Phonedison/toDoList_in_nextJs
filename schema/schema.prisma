generator client {
  provider = "prisma-client-js"
  outpot = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Tasks{
id String @id @default(cuid())
task String
done Boolean
}
